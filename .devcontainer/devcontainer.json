{
	"name": "connectedvcs-tools",
	"build": {
		// Sets the run context to one level up instead of the .devcontainer folder.
		"context": "..",
		// Update the 'dockerFile' property if you aren't using the standard 'Dockerfile' filename.
		"dockerfile": "../Dockerfile",
		"target": "mvn-build"
	},
	"features": {
		"ghcr.io/devcontainers/features/docker-in-docker:1": {
			"version": "latest"
		}
	},
	"postCreateCommand": "cd fedgov-cv-lib-asn1c && ./build_jni.sh --clean",
	"postStartCommand": "docker ps -q --filter ancestor=usdotfhwastol/connectedvcs-tools | xargs -r docker rm -f 2>/dev/null; docker build -t usdotfhwastol/connectedvcs-tools --build-arg USE_SSL=false /workspaces/connectedvcs-tools && docker run -d -p 8080:8080 usdotfhwastol/connectedvcs-tools",
	"forwardPorts": [],

	// Configure tool-specific properties
	"customizations": {
		"vscode": {
			"extensions": [
				"SonarSource.sonarlint-vscode"
			]
		}
	}
}